<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
xmlns:button="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="Store Control Panel"
             Routing.Route="StoreControlPanelRoute"
             x:Class="QuickOrderApp.Views.Store.StoreManger.StoreControlPanel">
    <ContentPage.Content>
        
            
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <!--<RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>-->
                </Grid.RowDefinitions>
            <Frame  
                Grid.Row="0" 
                HorizontalOptions="Fill"
                HeightRequest="150"                             
                Padding="0"
                IsClippedToBounds="True">

                <Image Source="{Binding Store.StoreImage,Converter={StaticResource BytesToImgConverter}}" Aspect="Fill" />
            </Frame>
            <Label Grid.Row="1" Text="{Binding Store.StoreName}" HorizontalOptions="Center" FontSize="Title" TextColor="Black"/>
            <BoxView HorizontalOptions="Fill" HeightRequest="0.1" BackgroundColor="Black" Grid.Row="2"></BoxView>

                <StackLayout Grid.Row="3" VerticalOptions="Center">

                <button:SfButton Text="Inventory" Grid.Row="3" CornerRadius="5" HasShadow="True" Margin="3" BackgroundColor="DodgerBlue" FontAttributes="Bold" Command="{Binding GoInventory}"/>
                <button:SfButton Text="Add Product" Grid.Row="4" CornerRadius="5" HasShadow="True" Margin="3" BackgroundColor="DodgerBlue" FontAttributes="Bold" Command="{Binding GoAddProduct}"/>
                <button:SfButton Text="Orders" Grid.Row="5" CornerRadius="5" HasShadow="True" Margin="3" BackgroundColor="DodgerBlue" FontAttributes="Bold" Command="{Binding GoOrdersCommand}"/>
                <button:SfButton Text="Employees" Grid.Row="6" CornerRadius="5" HasShadow="True" Margin="3" BackgroundColor="DodgerBlue" FontAttributes="Bold" Command="{Binding GoEmployeesCommand}"/>
                <button:SfButton Text="Search Employee" Grid.Row="7" CornerRadius="5" HasShadow="True" Margin="3" BackgroundColor="DodgerBlue" FontAttributes="Bold" Command="{Binding GoEmployeesCommand}"/>
                </StackLayout>
            </Grid>
     
    
    </ContentPage.Content>
</ContentPage>